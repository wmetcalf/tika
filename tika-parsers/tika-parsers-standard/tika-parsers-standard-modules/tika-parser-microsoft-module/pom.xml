<?xml version="1.0" encoding="UTF-8"?>
<!--
  Licensed to the Apache Software Foundation (ASF) under one
  or more contributor license agreements.  See the NOTICE file
  distributed with this work for additional information
  regarding copyright ownership.  The ASF licenses this file
  to you under the Apache License, Version 2.0 (the
  "License"); you may not use this file except in compliance
  with the License.  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing,
  software distributed under the License is distributed on an
  "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
  KIND, either express or implied.  See the License for the
  specific language governing permissions and limitations
  under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <artifactId>tika-parsers-standard-modules</artifactId>
    <groupId>org.apache.tika</groupId>
    <version>${revision}</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <modelVersion>4.0.0</modelVersion>

  <artifactId>tika-parser-microsoft-module</artifactId>
  <name>Apache Tika Microsoft parser module</name>

  <dependencies>
    <dependency>
      <groupId>de.thetaphi</groupId>
      <artifactId>forbiddenapis</artifactId>
      <version>${forbiddenapis.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <!-- needed for msg files -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-parser-html-module</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- UnicodeQRExtractor / UnicodeQRContentHandler — Unicode block-art QR
         decoding for ICS/MSG/EML/HTML/TXT bodies via ZXing-CPP. -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-parser-image-module</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- needed for msg files -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-parser-text-module</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- default chain for tests: html + mojibuster base detectors + junk arbiter -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-encoding-detector-html</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-encoding-detector-mojibuster</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-ml-junkdetect</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-parser-xml-module</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- needed for msg files -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-parser-mail-commons</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>com.pff</groupId>
      <artifactId>java-libpst</artifactId>
      <version>${libpst.version}</version>
    </dependency>
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-parser-zip-commons</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>commons-codec</groupId>
      <artifactId>commons-codec</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.poi</groupId>
      <artifactId>poi</artifactId>
    </dependency>
    <dependency>
      <groupId>org.apache.poi</groupId>
      <artifactId>poi-scratchpad</artifactId>
      <version>${poi.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.poi</groupId>
      <artifactId>poi-ooxml</artifactId>
      <exclusions>
        <exclusion>
          <groupId>org.apache.poi</groupId>
          <artifactId>poi-ooxml-lite</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <!-- needed by jackcess -->
    <dependency>
      <groupId>commons-logging</groupId>
      <artifactId>commons-logging</artifactId>
    </dependency>
    <dependency>
      <groupId>com.healthmarketscience.jackcess</groupId>
      <artifactId>jackcess</artifactId>
    </dependency>
    <dependency>
      <groupId>com.healthmarketscience.jackcess</groupId>
      <artifactId>jackcess-encrypt</artifactId>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcjmail-jdk18on</artifactId>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
    </dependency>
    <!-- needed for libpst test files -->
    <dependency>
      <groupId>${project.groupId}</groupId>
      <artifactId>tika-parser-mail-module</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>de.jflex</groupId>
        <artifactId>jflex-maven-plugin</artifactId>
        <version>1.9.1</version>
        <executions>
          <execution>
            <goals>
              <goal>generate</goal>
            </goals>
            <configuration>
              <lexDefinitions>
                <lexDefinition>src/main/jflex</lexDefinition>
              </lexDefinitions>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <configuration>
          <excludes>**/rtf/jflex/RTFTokenizer.java</excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.rat</groupId>
        <artifactId>apache-rat-plugin</artifactId>
        <configuration>
          <inputExcludes>
            <inputExclude>src/test/resources/test-documents/**</inputExclude>
          </inputExcludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <archive>
            <manifestEntries>
              <Automatic-Module-Name>org.apache.tika.parser.microsoft</Automatic-Module-Name>
            </manifestEntries>
          </archive>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>test-jar</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>report</id>
            <!--
              Re-bound from test to prepare-package. The parent binds the report to test, and
              within a phase Maven runs plugins in effective-POM order, so a parent-declared
              jacoco:report runs BEFORE a module-declared surefire execution: verified in the build
              log, report at line 545 and the cost-shape execution at 549. The report therefore
              omitted all 79 isolated tests. Moving it past every test execution restores them.

              Note the agent itself appends: getAppend() is getOption("append", true), so the
              forks add to target/jacoco.exec rather than replacing one another. Ordering, not
              append, was what lost the data.
            -->
            <phase>prepare-package</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!--
            Cost-SHAPE gates measure how cost GROWS with input, and that is only meaningful in a
            JVM whose heap and JIT state were not set by a thousand unrelated tests running first.
            Sharing one fork made them measure a different cost REGIME: the same gates pass
            standalone and fail inside the full suite, on changes that provably cannot reach them,
            because at large fixture sizes allocation rather than the work under test comes to
            dominate the timing.

            Two earlier attempts treated the INSTRUMENT as the problem, a better estimator
            (minimum-of-five, thread CPU time) and a smaller fixture. Each cut the failure rate
            without removing it, because neither addressed the shared JVM. This does.

            The default run excludes the tag; the execution below runs those classes alone with a
            fresh fork per class. Config is merged with the parent's, NOT overridden, so the
            inherited -Xmx4g argLine still applies.
          -->
          <excludedGroups>cost-shape</excludedGroups>
        </configuration>
        <executions>
          <execution>
            <id>cost-shape-tests</id>
            <phase>test</phase>
            <goals>
              <goal>test</goal>
            </goals>
            <configuration>
              <groups>cost-shape</groups>
              <!-- Must NAME a tag: an empty element does not clear the inherited exclusion, and
                   groups=cost-shape with excludedGroups=cost-shape silently matched NOTHING. -->
              <excludedGroups>not-a-real-tag</excludedGroups>
              <reuseForks>false</reuseForks>
              <failIfNoSpecifiedTests>false</failIfNoSpecifiedTests>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <scm>
    <tag>3.0.0-rc1</tag>
  </scm>
</project>
